import React from 'react';
import { Outlet } from 'react-router-dom';

const MainLayout = () => {
  /* Router */
  /* State */
  /* Functions */
  /* Hooks */
  /* Render */
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default MainLayout;
